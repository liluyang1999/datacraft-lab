<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.example.datacraft</groupId>
    <artifactId>datacraft-lab</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>datacraft-lab</name>
    <description>A personal JVM data processing factory built with Maven, Scala, Java, Spark, and Airflow orchestration.</description>
    <url>https://github.com/liluyang1999/datacraft-lab</url>
    <inceptionYear>2026</inceptionYear>

    <scm>
        <connection>scm:git:https://github.com/liluyang1999/datacraft-lab.git</connection>
        <developerConnection>scm:git:https://github.com/liluyang1999/datacraft-lab.git</developerConnection>
        <url>https://github.com/liluyang1999/datacraft-lab</url>
        <tag>HEAD</tag>
    </scm>

    <!-- Source modules are grouped by responsibility; design/architecture.md lists what each module
         owns and which project modules it may depend on (enforced per module by the Enforcer). -->
    <modules>
        <module>modules/core/datacraft-common</module>
        <module>modules/core/datacraft-config</module>
        <module>modules/io/datacraft-io</module>
        <module>modules/core/datacraft-engine</module>
        <module>modules/processing/datacraft-jobs</module>
        <module>modules/processing/datacraft-spark</module>
        <module>modules/interfaces/datacraft-api</module>
        <module>modules/interfaces/datacraft-cli</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <project.build.outputTimestamp>2026-01-01T00:00:00Z</project.build.outputTimestamp>

        <!-- Repository root (the directory holding .mvn). Tests live outside the modules, in
             tests/jvm/<artifactId>/{java,scala,resources}, so every module resolves them from here. -->
        <datacraft.root>${maven.multiModuleProjectDirectory}</datacraft.root>
        <datacraft.tests.dir>${datacraft.root}/tests/jvm/${project.artifactId}</datacraft.tests.dir>

        <java.version>25</java.version>
        <maven.compiler.release>${java.version}</maven.compiler.release>
        <maven.compiler.parameters>true</maven.compiler.parameters>
        <maven.compiler.showWarnings>true</maven.compiler.showWarnings>

        <scala.binary.version>2.13</scala.binary.version>
        <scala.version>2.13.18</scala.version>
        <spark.version>4.2.0</spark.version>
        <!-- Spark is treated as a runtime provided by the cluster / spark-submit. The
             'bundled-spark' profile flips this to compile to produce a self-contained local jar. -->
        <spark.scope>provided</spark.scope>

        <!-- JVM options a test JVM that embeds Spark needs: the java.base opens and JDK 24+ access
             flags from Spark 4.2's own launcher (org.apache.spark.launcher.JavaModuleOptions).
             Left out: the sun.security.action open (JDK 25 no longer has the package), the
             jdk.incubator.vector module (it prints an incubator warning), the Kerberos open and the
             Netty allocator and TLS defaults, which the tests do not use. spark-submit applies the
             launcher's full list at runtime. -->
        <spark.test.jvm.args>-XX:+IgnoreUnrecognizedVMOptions --add-opens=java.base/java.lang=ALL-UNNAMED --add-opens=java.base/java.lang.invoke=ALL-UNNAMED --add-opens=java.base/java.lang.reflect=ALL-UNNAMED --add-opens=java.base/java.io=ALL-UNNAMED --add-opens=java.base/java.net=ALL-UNNAMED --add-opens=java.base/java.nio=ALL-UNNAMED --add-opens=java.base/java.util=ALL-UNNAMED --add-opens=java.base/java.util.concurrent=ALL-UNNAMED --add-opens=java.base/java.util.concurrent.atomic=ALL-UNNAMED --add-opens=java.base/jdk.internal.ref=ALL-UNNAMED --add-opens=java.base/sun.nio.ch=ALL-UNNAMED --add-opens=java.base/sun.nio.cs=ALL-UNNAMED --add-opens=java.base/sun.util.calendar=ALL-UNNAMED --sun-misc-unsafe-memory-access=allow --enable-native-access=ALL-UNNAMED -Dio.netty.tryReflectionSetAccessible=true</spark.test.jvm.args>

        <!-- Options for every test JVM. NIO selectors create an AF_UNIX wakeup socket in
             jdk.net.unixdomain.tmpdir: some Windows profile TEMP trees cannot host one, which breaks
             HttpServer and Spark tests, so the module build directory is used on every OS. Logging
             is quiet (tests assert the failures they provoke); datacraft.test.log.level=info turns
             Spark's Log4j output back on. -->
        <datacraft.test.log.level>off</datacraft.test.log.level>
        <test.jvm.args>-Djdk.net.unixdomain.tmpdir=${project.build.directory} -Djava.util.logging.config.file=${datacraft.root}/tests/jvm/resources/logging.properties -Ddatacraft.test.log.level=${datacraft.test.log.level}</test.jvm.args>

        <!-- Platform-specific tests are tagged: posix-only (symbolic links without privilege, '?'
             in file names, Hadoop local writes without winutils) and windows-only (NTFS junctions).
             The windows-host and posix-host profiles exclude the other platform's tag, so no test
             reports as skipped or canceled; Linux CI runs every posix-only test. -->
        <test.excluded.tags></test.excluded.tags>

        <!-- A module whose JUnit tests all vanish (renamed class, provider mismatch) fails instead of
             passing green. Modules tested through scalatest skip Surefire (surefire.skip). An ad-hoc
             multi-module -Dtest=... run also needs -Dsurefire.failIfNoTests=false (with
             -Dsurefire.failIfNoSpecifiedTests=false) for modules without a matching test. -->
        <surefire.failIfNoTests>true</surefire.failIfNoTests>
        <surefire.skip>false</surefire.skip>

        <jackson.version>2.22.3</jackson.version>
        <jsch.version>2.28.7</jsch.version>
        <junit.jupiter.version>6.1.3</junit.jupiter.version>
        <scala.maven.plugin.version>4.9.10</scala.maven.plugin.version>
        <scalatest.version>3.2.20</scalatest.version>
        <scalatest.maven.plugin.version>2.2.0</scalatest.maven.plugin.version>
        <scopt.version>4.1.0</scopt.version>
        <spotless.version>3.10.3</spotless.version>
        <sshd.version>2.19.0</sshd.version>
        <slf4j.version>2.0.20</slf4j.version>
        <google.java.format.version>1.36.1</google.java.format.version>
        <!-- Checkstyle engine run by maven-checkstyle-plugin. The plugin's default engine (9.3)
             cannot parse Java 21/25 syntax such as record patterns, guarded switch labels and
             flexible constructor bodies; ModernSyntaxFixtureTest keeps that syntax in the build. -->
        <checkstyle.version>14.1.0</checkstyle.version>
        <maven.clean.plugin.version>3.5.0</maven.clean.plugin.version>
        <maven.checkstyle.plugin.version>3.6.0</maven.checkstyle.plugin.version>
        <maven.compiler.plugin.version>3.16.0</maven.compiler.plugin.version>
        <maven.dependency.plugin.version>3.11.0</maven.dependency.plugin.version>
        <maven.deploy.plugin.version>3.2.0</maven.deploy.plugin.version>
        <maven.enforcer.plugin.version>3.6.3</maven.enforcer.plugin.version>
        <maven.install.plugin.version>3.2.0</maven.install.plugin.version>
        <maven.jar.plugin.version>3.5.1</maven.jar.plugin.version>
        <maven.resources.plugin.version>3.5.0</maven.resources.plugin.version>
        <maven.shade.plugin.version>3.6.2</maven.shade.plugin.version>
        <maven.site.plugin.version>3.22.0</maven.site.plugin.version>
        <maven.surefire.plugin.version>3.6.0</maven.surefire.plugin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.scala-lang</groupId>
                <artifactId>scala-library</artifactId>
                <version>${scala.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.spark</groupId>
                <artifactId>spark-core_${scala.binary.version}</artifactId>
                <version>${spark.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.spark</groupId>
                <artifactId>spark-sql_${scala.binary.version}</artifactId>
                <version>${spark.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.spark</groupId>
                <artifactId>spark-hive_${scala.binary.version}</artifactId>
                <version>${spark.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson</groupId>
                <artifactId>jackson-bom</artifactId>
                <version>${jackson.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>com.github.scopt</groupId>
                <artifactId>scopt_${scala.binary.version}</artifactId>
                <version>${scopt.version}</version>
            </dependency>
            <dependency>
                <groupId>com.github.mwiede</groupId>
                <artifactId>jsch</artifactId>
                <version>${jsch.version}</version>
            </dependency>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${junit.jupiter.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.scalatest</groupId>
                <artifactId>scalatest_${scala.binary.version}</artifactId>
                <version>${scalatest.version}</version>
                <scope>test</scope>
            </dependency>
            <!-- Embedded SFTP server for SftpClient tests; sshd-core and sshd-common come transitively. -->
            <dependency>
                <groupId>org.apache.sshd</groupId>
                <artifactId>sshd-sftp</artifactId>
                <version>${sshd.version}</version>
                <scope>test</scope>
            </dependency>
            <!-- Selector-based transport for that test server: sshd's default NIO2 transport races its
                 own executor shutdown against Windows IOCP completions and prints uncaught errors. -->
            <dependency>
                <groupId>org.apache.sshd</groupId>
                <artifactId>sshd-mina</artifactId>
                <version>${sshd.version}</version>
                <scope>test</scope>
            </dependency>
            <!-- sshd logs through SLF4J; tests bind it to the no-op provider (one API line, 2.x). -->
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-nop</artifactId>
                <version>${slf4j.version}</version>
                <scope>test</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <!-- scala-maven-plugin derives its test directory as ${project.build.testSourceDirectory}/../scala. -->
        <testSourceDirectory>${datacraft.tests.dir}/java</testSourceDirectory>
        <testResources>
            <testResource>
                <directory>${datacraft.tests.dir}/resources</directory>
            </testResource>
            <testResource>
                <directory>${datacraft.root}/tests/jvm/resources</directory>
            </testResource>
        </testResources>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>${maven.clean.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>${maven.resources.plugin.version}</version>
                    <configuration>
                        <encoding>${project.build.sourceEncoding}</encoding>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven.compiler.plugin.version}</version>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                        <parameters>${maven.compiler.parameters}</parameters>
                        <showWarnings>${maven.compiler.showWarnings}</showWarnings>
                        <!-- Every javac lint category is on and any warning fails the build. -->
                        <compilerArgs>
                            <arg>-Xlint:all</arg>
                        </compilerArgs>
                        <failOnWarning>true</failOnWarning>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>net.alchim31.maven</groupId>
                    <artifactId>scala-maven-plugin</artifactId>
                    <version>${scala.maven.plugin.version}</version>
                    <configuration>
                        <scalaVersion>${scala.version}</scalaVersion>
                        <recompileMode>incremental</recompileMode>
                        <args>
                            <arg>-release:${maven.compiler.release}</arg>
                            <arg>-deprecation</arg>
                            <arg>-feature</arg>
                            <arg>-unchecked</arg>
                            <arg>-Xlint</arg>
                            <!-- Any scalac warning fails the build, like javac's failOnWarning. -->
                            <arg>-Werror</arg>
                        </args>
                    </configuration>
                    <executions>
                        <execution>
                            <goals>
                                <goal>compile</goal>
                                <goal>testCompile</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.scalatest</groupId>
                    <artifactId>scalatest-maven-plugin</artifactId>
                    <version>${scalatest.maven.plugin.version}</version>
                    <configuration>
                        <argLine>${test.jvm.args}</argLine>
                        <tagsToExclude>${test.excluded.tags}</tagsToExclude>
                        <reportsDirectory>${project.build.directory}/surefire-reports</reportsDirectory>
                        <junitxml>.</junitxml>
                        <filereports>${project.artifactId}-scalatest.txt</filereports>
                    </configuration>
                    <executions>
                        <execution>
                            <id>scala-test</id>
                            <goals>
                                <goal>test</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven.surefire.plugin.version}</version>
                    <configuration>
                        <argLine>${test.jvm.args}</argLine>
                        <excludedGroups>${test.excluded.tags}</excludedGroups>
                        <failIfNoTests>${surefire.failIfNoTests}</failIfNoTests>
                        <skip>${surefire.skip}</skip>
                        <trimStackTrace>false</trimStackTrace>
                        <useModulePath>false</useModulePath>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>${maven.jar.plugin.version}</version>
                    <configuration>
                        <archive>
                            <manifest>
                                <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                                <addDefaultSpecificationEntries>true</addDefaultSpecificationEntries>
                            </manifest>
                        </archive>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-shade-plugin</artifactId>
                    <version>${maven.shade.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-install-plugin</artifactId>
                    <version>${maven.install.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <version>${maven.deploy.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-dependency-plugin</artifactId>
                    <version>${maven.dependency.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-site-plugin</artifactId>
                    <version>${maven.site.plugin.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>${maven.enforcer.plugin.version}</version>
                <executions>
                    <execution>
                        <id>enforce-build-environment</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[25,)</version>
                                    <message>datacraft-lab builds with JDK 25 or newer and targets Java 25 bytecode (25.0.4.1+ recommended).</message>
                                </requireJavaVersion>
                                <requireMavenVersion>
                                    <version>[3.9.0,)</version>
                                </requireMavenVersion>
                                <requirePluginVersions>
                                    <banLatest>true</banLatest>
                                    <banRelease>true</banRelease>
                                    <banSnapshots>true</banSnapshots>
                                    <phases>clean,deploy,site</phases>
                                </requirePluginVersions>
                                <banDuplicatePomDependencyVersions />
                                <requireNoRepositories />
                                <!-- Named explicitly: jackson-annotations is versioned 2.22 (no patch
                                     number), which sorts below 2.22.3, so a group wildcard would ban it. -->
                                <bannedDependencies>
                                    <excludes>
                                        <exclude>com.fasterxml.jackson.core:jackson-core:[,2.22.3)</exclude>
                                        <exclude>com.fasterxml.jackson.core:jackson-databind:[,2.22.3)</exclude>
                                    </excludes>
                                    <message>jackson-core and jackson-databind older than 2.22.3 are affected by GHSA-wv8q-qhhj-9h54 (CVE-2026-91776), GHSA-cxp5-3px4-pw24 (CVE-2026-91777), GHSA-p6pp-m3f8-5c89 (CVE-2026-89407) and GHSA-7hhh-6rmp-j9qf (CVE-2026-89425).</message>
                                </bannedDependencies>
                            </rules>
                        </configuration>
                    </execution>
                    <!-- Warning only: the hard floor above stays [25,) so existing JDK 25 hosts keep building. -->
                    <execution>
                        <id>warn-deprecated-jdk</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[25.0.4.1,)</version>
                                    <level>WARN</level>
                                    <message>Use JDK 25.0.4.1 or newer: it carries the July/August 2026 security fixes, and Spark 4.2.0 deprecates Java 25 releases older than 25.0.3.</message>
                                </requireJavaVersion>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>${spotless.version}</version>
                <configuration>
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <lineEndings>UNIX</lineEndings>
                    <formats>
                        <format>
                            <includes>
                                <include>.gitattributes</include>
                                <include>*.md</include>
                                <include>*.xml</include>
                                <include>*.yml</include>
                                <include>*.yaml</include>
                                <include>*.properties</include>
                                <include>config/**/*.xml</include>
                                <include>docs/**/*.css</include>
                                <include>docs/**/*.html</include>
                                <include>docs/**/*.md</include>
                                <include>design/**/*.md</include>
                                <include>orchestration/**/*.py</include>
                                <include>tests/**/*.py</include>
                                <include>tests/**/*.properties</include>
                            </includes>
                            <trimTrailingWhitespace />
                            <endWithNewline />
                        </format>
                    </formats>
                    <java>
                        <!-- Module sources are checked in each module; the root project checks tests/. -->
                        <includes>
                            <include>src/main/java/**/*.java</include>
                            <include>tests/**/*.java</include>
                        </includes>
                        <googleJavaFormat>
                            <version>${google.java.format.version}</version>
                        </googleJavaFormat>
                        <removeUnusedImports />
                    </java>
                    <scala>
                        <includes>
                            <include>src/main/scala/**/*.scala</include>
                            <include>tests/**/*.scala</include>
                        </includes>
                        <scalafmt>
                            <version>3.11.5</version>
                            <file>${maven.multiModuleProjectDirectory}/.scalafmt.conf</file>
                            <scalaMajorVersion>${scala.binary.version}</scalaMajorVersion>
                        </scalafmt>
                    </scala>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>${maven.checkstyle.plugin.version}</version>
                <dependencies>
                    <dependency>
                        <groupId>com.puppycrawl.tools</groupId>
                        <artifactId>checkstyle</artifactId>
                        <version>${checkstyle.version}</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <configLocation>${maven.multiModuleProjectDirectory}/config/checkstyle/checkstyle.xml</configLocation>
                    <consoleOutput>true</consoleOutput>
                    <failsOnError>true</failsOnError>
                    <includeTestSourceDirectory>true</includeTestSourceDirectory>
                    <sourceDirectories>
                        <sourceDirectory>${project.basedir}/src/main/java</sourceDirectory>
                    </sourceDirectories>
                    <testSourceDirectories>
                        <testSourceDirectory>${project.build.testSourceDirectory}</testSourceDirectory>
                    </testSourceDirectories>
                </configuration>
                <executions>
                    <execution>
                        <id>checkstyle</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>windows-host</id>
            <activation>
                <os>
                    <family>windows</family>
                </os>
            </activation>
            <properties>
                <test.excluded.tags>posix-only</test.excluded.tags>
            </properties>
        </profile>
        <profile>
            <id>posix-host</id>
            <activation>
                <os>
                    <family>!windows</family>
                </os>
            </activation>
            <properties>
                <test.excluded.tags>windows-only</test.excluded.tags>
            </properties>
        </profile>
        <profile>
            <!-- Bundle Spark into the artifact for self-contained local runs (java -jar) instead of
                 spark-submit. Produces a large jar; intended only for laptop-scale experimentation. -->
            <id>bundled-spark</id>
            <properties>
                <spark.scope>compile</spark.scope>
            </properties>
        </profile>
    </profiles>
</project>
