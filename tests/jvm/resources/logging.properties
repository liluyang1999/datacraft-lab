# java.util.logging for test JVMs (passed as java.util.logging.config.file by the root pom).
# Tests assert the failures they provoke through their own handlers, so nothing is printed to the
# console. To debug a run, point java.util.logging.config.file at a config with a ConsoleHandler.
handlers=
.level=INFO
